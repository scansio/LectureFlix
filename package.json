{
  "name": "lectureflix",
  "version": "1.0.0",
  "description": "A combo of Netflix and YouTube",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "init": "cp .env-template .env && npm install",
    "init:database": "npm run migrate && npm run seed",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "node database_helper/migrate.js",
    "seed": "node database_helper/seed.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
